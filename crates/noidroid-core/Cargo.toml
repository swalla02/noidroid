[package]
name = "noidroid-core"
description = "Paranoid Android core: an immutable, content-addressed trajectory engine that records, reconstructs and branches executions."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
