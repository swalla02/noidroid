[package]
name = "noidroid-core"
description = "Immutable, content-addressed trajectory engine: record, reconstruct and branch executions."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
blake3 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
